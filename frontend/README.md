# Frontend (React)

Переменные окружения:
- `REACT_APP_API_URL` — базовый URL backend, по умолчанию `http://localhost:8000`

Запуск локально (без docker):

```bash
cd frontend
npm install
npm start
```

